# ═══════════════════════════════════════════════════════════════════
#  CI/CD Healing Agent — Environment Configuration (TEMPLATE)
# ═══════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your actual keys:
#   cp .env.example .env

# LLM Mode: set to "true" to use hardcoded dummy responses (no API key needed)
#           set to "false" to call real LLM API (requires a valid API key)
USE_DUMMY_LLM=false

# ─── LLM Provider: "openrouter", "cerebras", or "gemini" ───
LLM_PROVIDER=gemini

# ─── GitHub Token (required for pushing fixes to repos) ───
GITHUB_TOKEN=your_github_token_here

# ─── OpenRouter settings (used when LLM_PROVIDER=openrouter) ───
OPENROUTER_API_KEY=your_openrouter_key_here
OPENROUTER_MODEL=qwen/qwen3-next-80b-a3b-instruct:free

# ─── Cerebras settings (used when LLM_PROVIDER=cerebras) ───
CEREBRAS_API_KEY=your_cerebras_key_here
CEREBRAS_MODEL=qwen-3-235b-a22b-instruct-2507

# ─── Gemini settings (used when LLM_PROVIDER=gemini) ───
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-flash-latest
